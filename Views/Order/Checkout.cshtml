@model FigureShop.ViewModels.CheckoutViewModel

<div class="container py-5">
    <h2 class="text-center mb-4"><i class="fas fa-credit-card"></i> Thanh toán</h2>

    <form asp-action="Checkout" method="post" class="row g-4">
        <input type="hidden" name="selectedItemIds" value="@Context.Request.Query["selectedItemIds"]" />
        <!-- CỘT TRÁI: THÔNG TIN KHÁCH HÀNG -->
        <div class="col-md-6">
            <div class="card shadow-sm mb-4" style="border-radius: 18px;">
                <div class="card-body">
                    <div class="row g-3">
                        <div class="col-12">
                            <label asp-for="FullName" class="form-label">Họ tên</label>
                            <input asp-for="FullName" class="form-control" required style="border-radius: 12px;" />
                        </div>
                        <div class="col-12">
                            <label asp-for="Address" class="form-label">Địa chỉ nhận hàng</label>
                            <input asp-for="Address" class="form-control" required style="border-radius: 12px;" />
                        </div>
                        <div class="col-12">
                            <label asp-for="Phone" class="form-label">Số điện thoại</label>
                            <input asp-for="Phone" class="form-control" required style="border-radius: 12px;" />
                        </div>
                        <div class="col-12">
                            <label asp-for="Note" class="form-label">Ghi chú</label>
                            <textarea asp-for="Note" class="form-control" rows="3" style="border-radius: 12px;"></textarea>
                        </div>
                        <div class="col-12">
                            <label asp-for="PaymentMethod" class="form-label">Phương thức thanh toán</label>
                            <select asp-for="PaymentMethod" class="form-select" id="paymentMethod" style="border-radius: 12px;">
                                <option value="COD">Thanh toán khi nhận hàng (COD)</option>
                                <option value="Bank">Chuyển khoản ngân hàng</option>
                            </select>
                        </div>
                    </div>
                </div>
            </div>
            <div class="card shadow-sm mb-4" style="border-radius: 18px;">
                <div class="card-body">
                    <div class="d-flex align-items-center mb-2">
                        <i class="fa fa-ticket-alt fa-lg text-warning me-2"></i>
                        <span class="fw-bold" style="font-size: 1.1em;">Mã giảm giá / Voucher</span>
                    </div>
                    <div class="row g-2 align-items-center">
                        <div class="col-8">
                            <select class="form-select" id="voucherSelect" name="VoucherCode" style="border-radius: 12px;">
                                <option value="">-- Chọn mã giảm giá --</option>
                                <option value="NEW50">Giảm 50,000₫ cho người mới</option>
                                <option value="FREESHIP">Miễn phí vận chuyển</option>
                            </select>
                        </div>
                        <div class="col-4 text-end">
                            <button type="button" class="btn btn-warning w-100 fw-bold" style="border-radius: 12px;" onclick="applyVoucher()">
                                <i class="fa fa-gift me-1"></i> Áp dụng mã
                            </button>
                        </div>
                    </div>
                    <div id="voucherMessage" class="mt-2 text-success small"></div>
                </div>
            </div>

            <!-- THÔNG TIN CHUYỂN KHOẢN + QR -->
           <div id="bankTransferInfo" class="alert alert-info d-none shadow-sm">
    <div class="row align-items-center">
        <!-- THÔNG TIN -->
        <div class="col-md-8">
            <h6 class="mb-2 text-primary">
                <i class="fas fa-university me-1"></i> Thông tin chuyển khoản
            </h6>
            <ul class="list-unstyled small mb-2">
                <li>🏦 Ngân hàng: <strong>VietinBank</strong></li>
                <li>💳 Số tài khoản: <strong>107874868604</strong></li>
                <li>👤 Chủ tài khoản: <strong>HOANG NGOC NAM KIET</strong></li>
                <li>📝 Nội dung chuyển khoản: <code class="fw-bold text-dark">Họ và Tên</code></li>
            </ul>
            <div class="small mt-2">
                📸 Sau khi chuyển, vui lòng <strong>chụp ảnh</strong> và gửi qua Zalo:
                <a href="https://zalo.me/0961855997" target="_blank" class="fw-bold text-primary text-decoration-none">
                    0961855997
                </a>
            </div>
        </div>

        <!-- QR CODE -->
        <div class="col-md-4 text-center">
            <img src="~/images/banking.jpg" alt="QR chuyển khoản"
                 class="img-thumbnail rounded shadow-sm" style="max-width: 130px;" />
            <div class="small text-muted mt-1">📲 Quét mã QR</div>
        </div>
    </div>
</div>

            <button type="submit" class="btn btn-primary btn-lg w-100 mt-3">
                <i class="fas fa-paper-plane"></i> Đặt hàng
            </button>
        </div>

        <!-- CỘT PHẢI: GIỎ HÀNG -->
        <div class="col-md-6">
            <h5 class="mb-3"><i class="fas fa-box"></i> Giỏ hàng của bạn</h5>
            <div class="table-responsive">
                <table class="table table-bordered align-middle shadow-sm" style="border-radius: 18px; overflow: hidden;">
                    <thead class="table-light">
                        <tr>
                            <th class="text-center" style="font-size: 1.1em;">Sản phẩm</th>
                            <th class="text-center" style="width: 60px;">SL</th>
                            <th class="text-end" style="min-width: 120px;">Đơn giá</th>
                            <th class="text-end" style="min-width: 130px;">Tổng</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.CartItems)
                        {
                            <tr>
                                <td>
                                    <div class="d-flex align-items-center gap-2">
                                        <img src="@(string.IsNullOrEmpty(item.Product.ImagePath) ? "/images/no-image.png" : item.Product.ImagePath)" alt="@item.Product.Name"
                                             style="width: 48px; height: 48px; object-fit: contain; background: #f8f9fa; border-radius: 10px; box-shadow: 0 2px 8px #eee;" onerror="this.src='/images/no-image.png'" />
                                        <span class="small fw-semibold">@item.Product.Name</span>
                                    </div>
                                </td>
                                <td class="text-center align-middle">@item.Quantity</td>
                                <td class="text-end align-middle">@item.Price.ToString("N0") ₫</td>
                                <td class="text-end align-middle">@((item.Quantity * item.Price).ToString("N0")) ₫</td>
                            </tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr style="background: #f8f9fa;">
                            <th colspan="3" class="text-end" style="border-radius: 12px 0 0 12px; font-size: 1.1em;">Tổng cộng:</th>
                            <th class="text-end text-danger fw-bold" id="totalAmount" style="font-size: 1.2em; border-radius: 0 12px 12px 0; background: #fff3f3;">@Model.TotalAmount.ToString("N0") ₫</th>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </form>
</div>

@section Scripts {
<script>
    document.addEventListener("DOMContentLoaded", function () {
        const methodSelect = document.getElementById("paymentMethod");
        const bankInfo = document.getElementById("bankTransferInfo");

        function toggleBankInfo() {
            if (methodSelect.value === "Bank") {
                bankInfo.classList.remove("d-none");
            } else {
                bankInfo.classList.add("d-none");
            }
        }

        methodSelect.addEventListener("change", toggleBankInfo);
        toggleBankInfo(); // Gọi lần đầu khi trang tải
    });

    let discount = 0;
    const originalTotal = @Model.TotalAmount;
    function applyVoucher() {
        var code = document.getElementById('voucherSelect').value;
        var msg = '';
        discount = 0;
        if (code === 'NEW50') {
            discount = 50000;
            msg = 'Đã áp dụng mã giảm 50,000₫ cho người mới.';
        } else if (code === 'FREESHIP') {
            discount = 30000;
            msg = 'Đã áp dụng mã miễn phí vận chuyển.';
        } else {
            msg = '';
        }
        document.getElementById('voucherMessage').innerText = msg;
        updateTotal();
    }
    function updateTotal() {
        var total = originalTotal - discount;
        document.getElementById('totalAmount').innerText = total.toLocaleString() + ' ₫';
    }
</script>

<script src="https://maps.googleapis.com/maps/api/js?key=YOUR_API_KEY&callback=initMap" async defer></script>
<script>
    let map, marker;

    function initMap() {
        const defaultLocation = { lat: 10.762622, lng: 106.660172 }; // Default to Ho Chi Minh City
        map = new google.maps.Map(document.getElementById('map'), {
            center: defaultLocation,
            zoom: 13,
        });

        marker = new google.maps.Marker({
            position: defaultLocation,
            map: map,
            draggable: true,
        });

        google.maps.event.addListener(marker, 'dragend', function () {
            const position = marker.getPosition();
            document.getElementById('latitude').value = position.lat();
            document.getElementById('longitude').value = position.lng();
        });
    }
</script>
}
